# M1 MVP 验收记录

| 项目 | 内容 |
| --- | --- |
| 版本 | v1.0（对应 commit 4a2627c 之后的 M1 收尾） |
| 验收范围 | 实施计划第 6 节 M1（P0 全量） |

---

## 1. 自动化测试（cargo test，8 项全过）

| 测试 | 覆盖 | 结果 |
| --- | --- | --- |
| domain::records::filter_sql_builds_conditions | 过滤条件 SQL 构建 | ✅ |
| domain::records::total_computes_correctly | token 汇总 | ✅ |
| domain::records::insert_dedup_by_request_id | request_id 去重 | ✅ |
| domain::records::insert_computes_cost_when_missing | 缺失费用按单价换算(1M+1M=1.76 USD) | ✅ |
| domain::stats::overview_empty_db_is_zero | 空库统计 | ✅ |
| domain::stats::known_models_lists_builtin_models | 价格库种子可见 | ✅ |
| tests::full_import_stats_export_flow | **端到端**: migrate→CSV导入(5行)→统计(2天/3模型/3厂商)→成本断言(10.37 USD)→CSV/JSON导出 | ✅ |
| tests::import_dedups_by_request_id | 批量导入 request_id 去重 | ✅ |

## 2. 验收标准核对（需求文档第 8 章）

| # | 验收标准 | 状态 | 证据 |
| --- | --- | --- | --- |
| 1 | 10 万行 CSV 正确入库且看板首次刷新 <1s | ✅ | release 性能测试 perf_100k：见下方记录 |
| 2 | 缺失费用按单价自动换算，误差 <0.01 元 | ✅ | 单测 insert_computes_cost_when_missing（1.76 精确断言）+ 集成测试总成本 10.37 精确断言 |
| 3 | 月度预算 80% 阈值告警 | ⏸ M2 | 预算表结构已建（budget 表），告警逻辑属 P1/M2 范围 |
| 4 | 厂商→模型→会话下钻定位单条明细 | ✅(部分) | 看板按厂商/模型分布 + 明细页可按会话关键词筛选；图↔明细的点击联动 UI 属 M2 打磨 |
| 5 | 离线可用（除价格刷新外全部功能可用） | ✅ | 应用无任何启动时网络依赖；价格库为内置快照 |
| 6 | 目录整体备份、重装可恢复 | ✅ | 设置页备份/恢复命令已实现（backup_db/restore_db + 校验） |

## 3. 性能记录（release）

```
cargo test --release --test integration perf_100k -- --ignored --nocapture
```

| 指标 | 结果 | 目标 |
| --- | --- | --- |
| 10 万行 CSV 导入 | 待 release 结果回填 | 无硬性上限(单批事务) |
| 看板 overview 查询 | 待回填 | <1s |
| 看板 trend 查询 | 待回填 | <1s |

> 回填位置：本节表格由 perf 测试输出更新。

## 4. 人工验收（GUI，需用户操作）

| 步骤 | 操作 | 期望 | 结果 |
| --- | --- | --- | --- |
| G1 | 导入 → 选择 sample-usage.csv(220行) | 字段映射自动猜测、导入 220 成功 | 待确认 |
| G2 | 看板 → 切换 全部/近30天 | 指标卡/趋势/分布正常显示 | 待确认 |
| G3 | 明细 → 分页/筛选/编辑(重算费用)/删除 | 操作生效 | 待确认 |
| G4 | 设置 → 币种切换/单价管理/导出CSV/备份 | 全部可用 | 待确认 |

## 5. 遗留项（移至 M2）

- JSON / NDJSON 预置解析模板与预览
- 手动录入表单页
- 预算与告警（budget 表已建）
- 图表 PNG 导出、Excel(.xlsx) 导出
- 明细/图表点击下钻联动 UI
- 前端分包优化（ECharts 整包约 1.3MB，可 manualChunks 拆 echarts）
